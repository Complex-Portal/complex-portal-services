db.hbm2ddl=${db.hbm2ddl}
db.dialect=${db.dialect}
db.driver=${db.driver}
db.url=${db.url}
db.user=${db.user}
db.password=${db.password}

input.file.name=${input.file.name}
output.directory=${output.directory}
process.output.dir.name=${process.output.dir.name}
separator=${separator}
header=${header}

database.id=${database.id}

jami.user.context.id=${jami.user.context.id}
jami.ac.prefix=EBI

dry.run.mode=true
